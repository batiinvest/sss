{
  "name": "sss-study-fund",
  "version": "1.0.0",
  "private": true,
  "description": "SSS study fund web app packaged with Capacitor",
  "scripts": {
    "cap:prepare": "node scripts/prepare-capacitor-web.mjs",
    "cap:sync": "npm run cap:prepare && cap sync ios",
    "cap:open:ios": "npm run cap:sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
